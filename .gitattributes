# Default behavior: normalize text files to LF
* text=auto eol=lf

# Ensure JSON files are treated as text (better merge/diff)
*.json text

# CSS, JS, HTML should use UTF-8 and normalize to LF
*.js text working-tree-encoding=UTF-8
*.css text working-tree-encoding=UTF-8
*.html text working-tree-encoding=UTF-8

# Ignore diff for minified JS files
*.min.js -diff

# Treat images as binary
*.png binary
*.jpg binary
*.jpeg binary
